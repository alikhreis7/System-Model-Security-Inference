# Large model checkpoints (not needed in repo — regenerated by training scripts)
models/biencoder_best.pt
models/biencoder_tokenizer/

# Old data split files (superseded by lookup+classifier approach)
data/train_pairs.json
data/val_pairs.json
data/test_pairs.json
